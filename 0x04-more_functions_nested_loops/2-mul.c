#include "main.h"
/**
 * mul - multiplies two integ
 * @a: the first integ to multiply
 * @b:the second integ to multiply with the first
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
return (a * b);
}
