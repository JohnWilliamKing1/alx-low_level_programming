#include <stdio.h>
/**
 * _strchr - entry point
 * @s: input
 * @c: input
 * Return: always 0 (success)
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
int i;
for (i = 0; s[i] >= '\0'; i++)
{
if (s[i] == c)
return (s+i);
}
return (NULL);
}
